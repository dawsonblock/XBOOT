# Fill these values before building templates from source.
# The build scripts refuse to proceed when required values remain unset.
firecracker_version=REQUIRED
kernel_version=REQUIRED
kernel_sha256=REQUIRED
kernel_path=guest/vmlinux-fc
arch=x86_64
